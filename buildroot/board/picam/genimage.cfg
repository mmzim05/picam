image sdcard.img {
    hdimage {
        gpt = false
    }

    partition boot {
        partition-type = 0xC       # FAT32 LBA
        bootable = true
        image = "boot.vfat"
    }

    partition rootfs {
        partition-type = 0x83      # Linux
        image = "rootfs.squashfs"
    }
}
